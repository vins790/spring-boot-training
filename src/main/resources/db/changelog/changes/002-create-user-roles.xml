<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="002-1" author="system">
        <comment>Create user_roles table for storing user roles</comment>
        <createTable tableName="user_roles">
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="role" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addPrimaryKey
            tableName="user_roles"
            columnNames="user_id, role"
            constraintName="pk_user_roles"/>
        <addForeignKeyConstraint
            baseTableName="user_roles"
            baseColumnNames="user_id"
            constraintName="fk_user_roles_user"
            referencedTableName="users"
            referencedColumnNames="user_id"
            onDelete="CASCADE"/>
        <createIndex tableName="user_roles" indexName="idx_user_roles_role">
            <column name="role"/>
        </createIndex>
    </changeSet>

    <changeSet id="002-2" author="system">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">SELECT COUNT(*) > 0 FROM users</sqlCheck>
        </preConditions>
        <comment>Insert default USER role for all existing users</comment>
        <sql>
            INSERT INTO user_roles (user_id, role)
            SELECT user_id, 'USER' FROM users
            WHERE user_id NOT IN (SELECT user_id FROM user_roles)
        </sql>
        <rollback>
            DELETE FROM user_roles WHERE role = 'USER'
        </rollback>
    </changeSet>

</databaseChangeLog>
